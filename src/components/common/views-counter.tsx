// src/components/common/views-counter.tsx
export function ViewsCounter({ slug, increment }: { slug: string; increment?: boolean }) {
    // nanti bisa connect ke Firestore/Redis kalau perlu
    return <span className="text-xs opacity-70">123</span>
  }